import { call, put, takeEvery } from "redux-saga/effects";

function* workUpdateState() {}

function* appSaga() {
  //   yield takeEvery("appReducer/updateState", workUpdateState);
}

export default appSaga;
